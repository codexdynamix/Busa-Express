# Database connection (optional - defaults to embedded PGlite in-memory Postgres)
DATABASE_URL=

# Gemini API Key (for server-side AI logic if needed)
GEMINI_API_KEY=

# Auth toggle
VITE_AUTH_ENABLED=true
